# FastAPI backend configuration
SECRET_KEY=change-me
# FERNET_KEY is optional but recommended: generate with `python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"`
FERNET_KEY=
DATABASE_URL=sqlite:///backend/data/app.db
FRONTEND_ORIGIN=http://localhost:3000

# Engines (none | vosk | whispercpp | openai)
STT_ENGINE=none
OPENAI_API_KEY=

# Engines (none | pyttsx3 | coqui | elevenlabs | openai)
TTS_ENGINE=none
ELEVENLABS_API_KEY=

# Wake word
WAKE_WORD=hey vaani
